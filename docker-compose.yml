services:
  web-client:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - session-data:/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/sessions.db
    restart: unless-stopped

volumes:
  session-data:
